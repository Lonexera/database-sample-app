<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_filter"
        android:icon="@drawable/ic_baseline_filter_alt_24"
        android:title="@string/toolbar_filter_title"
        android:contentDescription="@string/toolbar_filter_description"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/settings"
        android:title="@string/toolbar_settings_title"
        android:contentDescription="@string/toolbar_settings_description"
        app:showAsAction="never"/>

</menu>